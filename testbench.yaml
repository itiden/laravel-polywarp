laravel: "./workbench"

providers:
  # - Workbench\App\Providers\WorkbenchServiceProvider

migrations:

seeders:

workbench:
  start: "/"
  install: true
  health: false
  discovers:
    commands: true
  build:
  assets:
  sync:
    - from: lang
      to: workbench/lang
      reverse: true
    - from: storage
      to: workbench/storage
      reverse: true
